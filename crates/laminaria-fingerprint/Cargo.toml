[package]
name = "laminaria-fingerprint"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Environment and toolchain fingerprinting primitives for LAMINARIA (issue #18)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
toml = "0.8"
